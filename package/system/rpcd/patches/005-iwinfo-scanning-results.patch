--- a/iwinfo.c
+++ b/iwinfo.c
@@ -470,6 +470,14 @@ rpc_iwinfo_scan(struct ubus_context *ctx
 			blobmsg_close_table(&buf, d);
 		}
 	}
+	else
+	{
+		d = blobmsg_open_table(&buf, NULL);
+
+		blobmsg_add_string(&buf, "status", "Scanning not possible");
+
+		blobmsg_close_table(&buf, d);
+	}
 
 	blobmsg_close_array(&buf, c);
 
