--- a/utils/askfirst.c
+++ b/utils/askfirst.c
@@ -23,11 +23,15 @@ int main(int argc, char **argv)
 {
 	int c;
 
+	sleep(1);
+
 	printf("Please press Enter to activate this console.\n");
 	do {
 		c = getchar();
-		if (c == EOF)
+		if (c == EOF) {
+			sleep(1);
 			return -1;
+		}
 	}
 	while (c != 0xA);
 
