--- a/iwinfo_wext.c
+++ b/iwinfo_wext.c
@@ -57,8 +57,8 @@ static int wext_probe(const char *ifname
 {
 	struct iwreq wrq;
 
-	if(wext_ioctl(ifname, SIOCGIWNAME, &wrq) >= 0)
-		return 1;
+	//if(wext_ioctl(ifname, SIOCGIWNAME, &wrq) >= 0)
+	//	return 1;
 
 	return 0;
 }
