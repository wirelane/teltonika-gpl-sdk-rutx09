--- a/device.c
+++ b/device.c
@@ -504,7 +504,7 @@ device_fill_default_settings(struct devi
 		ea = config_get_default_macaddr(dev->ifname);
 		if (ea) {
 			memcpy(s->macaddr, ea, 6);
-			s->flags |= DEV_OPT_DEFAULT_MACADDR;
+			s->flags |= DEV_OPT_MACADDR;
 		}
 	}
 }
