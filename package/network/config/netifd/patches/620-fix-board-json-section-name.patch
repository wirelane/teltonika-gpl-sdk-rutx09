--- a/config.c
+++ b/config.c
@@ -919,7 +919,7 @@ config_init_board(void)
 	free(board_netdevs);
 	board_netdevs = NULL;
 
-	cur = config_find_blobmsg_attr(b.head, "network_device",
+	cur = config_find_blobmsg_attr(b.head, "network-device",
 				       BLOBMSG_TYPE_TABLE);
 	if (!cur)
 		return;
