--- a/config.c
+++ b/config.c
@@ -910,6 +910,7 @@ config_init_all(void)
 	device_reset_old();
 	device_init_pending();
 	vlist_flush(&interfaces);
+	device_free_unused();
 	interface_refresh_assignments(false);
 	interface_start_pending();
 	wireless_start_pending(0);
