Index: odhcpd-2021-07-18-bc9d317f/src/odhcpd.c
===================================================================
--- odhcpd-2021-07-18-bc9d317f.orig/src/odhcpd.c
+++ odhcpd-2021-07-18-bc9d317f/src/odhcpd.c
@@ -92,11 +92,6 @@ int main(int argc, char **argv)
 	setlogmask(LOG_UPTO(config.log_level));
 	uloop_init();
 
-	if (getuid() != 0) {
-		syslog(LOG_ERR, "Must be run as root!");
-		return 2;
-	}
-
 	ioctl_sock = socket(AF_INET, SOCK_DGRAM | SOCK_CLOEXEC, 0);
 
 	if (ioctl_sock < 0)
