#!/bin/sh

CONFIG_PATH="${UCI_CONFIG_DIR:-"/etc/config"}"

sed -i -E "s/(config fwd_to_)([a-z]+)(.*)/config fwd \'to_\2\'/" "$CONFIG_PATH/sms_gateway"
sed -i -E "s/(config reply)(.*)/config fwd 'reply'\2/" "$CONFIG_PATH/sms_gateway"
