if PACKAGE_kmod-cypress-serial

config CYPRESS_SERIAL_MODPARAMS
	string "Default cypress usb to serial converter module arguments"
	default "path_only=1-1.3" if DEFAULT_kmod-cypress-serial
	default "cdc=y"

endif
