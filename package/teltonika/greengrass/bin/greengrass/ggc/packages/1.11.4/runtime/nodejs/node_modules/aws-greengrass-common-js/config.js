/*
 * Copyright 2010-2018 Amazon.com, Inc. or its affiliates. All Rights Reserved.
 */

const http = require('http');

const config = {};
config.DEFAULT_HTTP_AGENT = new http.Agent({ keepAlive: true });
config.DEFAULT_HOST = 'localhost';
/**
 * @deprecated do not use this hard-coded value. Use the environment variable.
 * @see envVars.GG_DAEMON_PORT
 * @type {number}
 */
config.DEFAULT_PORT = 8000;

module.exports = config;
