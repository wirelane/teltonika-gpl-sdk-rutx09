Index: backports-6.12.6/net/mac80211/mlme.c
===================================================================
--- backports-6.12.6/net/mac80211/mlme.c
+++ backports-6.12.6/net/mac80211/mlme.c
@@ -35,6 +35,7 @@
 #define IEEE80211_AUTH_TIMEOUT_LONG	(HZ / 2)
 #define IEEE80211_AUTH_TIMEOUT_SHORT	(HZ / 10)
 #define IEEE80211_AUTH_TIMEOUT_SAE	(HZ * 2)
+#define IEEE80211_AUTH_TIMEOUT_FT	HZ
 #define IEEE80211_AUTH_MAX_TRIES	3
 #define IEEE80211_AUTH_WAIT_ASSOC	(HZ * 5)
 #define IEEE80211_AUTH_WAIT_SAE_RETRY	(HZ * 2)
@@ -7730,6 +7731,11 @@ void ieee80211_sta_work(struct ieee80211
 					ifmgd->auth_data->timeout =
 						jiffies +
 						IEEE80211_AUTH_TIMEOUT_SAE;
+				else if (ifmgd->auth_data->algorithm ==
+					WLAN_AUTH_FT)
+					ifmgd->auth_data->timeout =
+						jiffies +
+						IEEE80211_AUTH_TIMEOUT_FT;
 				else
 					ifmgd->auth_data->timeout =
 						jiffies +
